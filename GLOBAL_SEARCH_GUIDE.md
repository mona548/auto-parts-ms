# دليل نظام البحث العام (Global Search)

## نظرة عامة
نظام بحث شامل وسريع يتيح للمستخدم البحث في جميع بيانات النظام من مكان واحد.

---

## الملفات المضافة

### 1. `/src/app/components/GlobalSearch.tsx`
مكون البحث الرئيسي مع Command Dialog.

**المكونات المصدّرة:**
- `GlobalSearch` - المكون الرئيسي (المودال)
- `GlobalSearchTrigger` - زر تفعيل البحث

---

## المميزات الرئيسية

### ⌨️ اختصار لوحة المفاتيح
```
Ctrl + K (Windows/Linux)
Cmd + K (Mac)
```
يفتح نافذة البحث مباشرة من أي صفحة.

### 🔍 البحث الشامل
يبحث في **ثلاثة أنواع** من البيانات في نفس الوقت:

#### 1. قطع الغيار (Parts) 📦
**يبحث في:**
- اسم القطعة
- كود المارايه (partCode)
- الفئة (category)

**مثال:**
```
البحث: "فلتر"
النتائج:
- فلتر زيت تويوتا كورولا
- فلتر هواء نيسان
- الفئة: فلاتر
```

#### 2. العملاء (Customers) 👥
**يبحث في:**
- اسم العميل
- رقم الهاتف

**مثال:**
```
البحث: "أحمد" أو "0500"
النتائج:
- أحمد محمد
- محمد أحمد
```

#### 3. الفواتير (Invoices) 📄
**يبحث في:**
- رقم الفاتورة (invoiceNumber)
- اسم العميل في الفاتورة

**مثال:**
```
البحث: "INV-1001" أو اسم العميل
النتائج:
- INV-1001 • أحمد محمد • 1,500 ريال
```

---

## واجهة المستخدم

### 🎨 التصميم

**Header (شريط البحث):**
```
┌─────────────────────────────────────┐
│ 🔍 ابحث...              Ctrl+K    │
└─────────────────────────────────────┘
```

**Command Dialog (نافذة البحث):**
```
┌────────────────────────────────────────────┐
│ 🔍 ابحث عن قطع غيار، عملاء...    ✕  ESC │
├────────────────────────────────────────────┤
│                                            │
│  📦 قطع الغيار (5)                        │
│  ├─ فلتر زيت تويوتا كورولا                │
│  │   فلاتر • متوفر • 45 قطعة               │
│  └─ ...                                    │
│                                            │
│  👥 العملاء (3)                            │
│  ├─ أحمد محمد                              │
│  │   📞 0500000000                         │
│  └─ ...                                    │
│                                            │
│  📄 الفواتير (2)                           │
│  ├─ INV-1001                               │
│  │   أحمد محمد • 1,500 ريال                │
│  └─ ...                                    │
├────────────────────────────────────────────┤
│ ↑↓ للتنقل  Enter للاختيار      ESC للإغلاق│
└────────────────────────────────────────────┘
```

### 🎨 الألوان حسب النوع

| النوع | اللون | الأيقونة |
|-------|-------|----------|
| قطع الغيار | أزرق (#3B82F6) | 📦 Package |
| العملاء | أخضر (#22C55E) | 👥 Users |
| الفواتير | بنفسجي (#A855F7) | 📄 FileText |

### ✨ التفاعلات (Hover)

**عند التمرير فوق نتيجة:**
- قطعة غيار → خلفية زرقاء فاتحة
- عميل → خلفية خضراء فاتحة
- فاتورة → خلفية بنفسجية فاتحة

---

## كيفية الاستخدام

### 1. فتح نافذة البحث

**الطريقة الأولى:** اختصار لوحة المفاتيح
```
اضغط Ctrl+K (أو Cmd+K على Mac)
```

**الطريقة الثانية:** زر البحث
```
اضغط على زر "ابحث... (Ctrl+K)" في الأعلى
```

### 2. البحث

**اكتب أي شيء:**
```
- اسم قطعة: "فلتر"
- كود: "P001"
- اسم عميل: "أحمد"
- رقم هاتف: "0500"
- رقم فاتورة: "INV-1001"
```

**النتائج تظهر فوراً** أثناء الكتابة (Live Search)

### 3. اختيار نتيجة

**الطريقة الأولى:** الفأرة
```
اضغط على النتيجة المطلوبة
```

**الطريقة الثانية:** لوحة المفاتيح
```
1. استخدم ↑ و ↓ للتنقل بين النتائج
2. اضغط Enter للاختيار
```

### 4. الإغلاق

```
- اضغط ESC
- أو اضغط خارج النافذة (على الخلفية المعتمة)
- أو اضغط زر ✕
```

---

## السلوك عند الاختيار

### 📦 قطعة غيار
```
ينتقل إلى → /inventory
الإجراء: يُبرز القطعة في الجدول (highlight)
```

**كيف يعمل:**
```typescript
// يحفظ ID في sessionStorage
sessionStorage.setItem("highlightPart", partId);

// في صفحة Inventory، يقرأ ويُبرز
const highlightId = sessionStorage.getItem("highlightPart");
```

### 👥 عميل
```
ينتقل إلى → /customers/{customerId}
الإجراء: يفتح صفحة ملف العميل مباشرة
```

### 📄 فاتورة
```
ينتقل إلى → /invoices
الإجراء: يُبرز الفاتورة ويفتح تفاصيلها (اختياري)
```

**كيف يعمل:**
```typescript
// يحفظ ID في sessionStorage
sessionStorage.setItem("highlightInvoice", invoiceId);

// في صفحة Invoices، يقرأ ويفتح Dialog
```

---

## البيانات المصدر

### قطع الغيار
```typescript
// من PartsContext
const { parts } = useParts();
```

### العملاء
```typescript
// من localStorage مباشرة
const customers = JSON.parse(localStorage.getItem("customers") || "[]");
```

### الفواتير
```typescript
// من localStorage مباشرة
const invoices = JSON.parse(localStorage.getItem("invoices") || "[]");
```

---

## الحد الأقصى للنتائج

```typescript
setResults(searchResults.slice(0, 15)); // 15 نتيجة فقط
```

**لماذا 15؟**
- أداء أفضل
- تجربة مستخدم أفضل (لا scroll طويل)
- إذا كانت النتائج كثيرة، يجب تحسين البحث

---

## التقنيات المستخدمة

### React Hooks
```typescript
useState   // إدارة حالة المودال والنتائج
useEffect  // اختصار لوحة المفاتيح والبحث
```

### Navigation
```typescript
useNavigate() // من react-router للانتقال بين الصفحات
```

### Local Storage
```typescript
localStorage.getItem("customers")
localStorage.getItem("invoices")
sessionStorage.setItem("highlightPart", id)
```

### Event Listeners
```typescript
// Ctrl+K
document.addEventListener("keydown", handleKeyDown);

// ESC
if (e.key === "Escape") setOpen(false);
```

---

## أمثلة البحث

### مثال 1: البحث عن قطعة
```
الإدخال: "فلتر زيت"

النتائج:
📦 قطع الغيار (2)
├─ فلتر زيت تويوتا كورولا
│   فلاتر • متوفر • 45 قطعة
└─ فلتر زيت هوندا
    فلاتر • منخفض • 8 قطع
```

### مثال 2: البحث برقم الهاتف
```
الإدخال: "050"

النتائج:
👥 العملاء (3)
├─ أحمد محمد
│   📞 0500000000
├─ فاطمة علي
│   📞 0501111111
└─ محمود حسن
    📞 0502222222
```

### مثال 3: البحث بالفئة
```
الإدخال: "فرامل"

النتائج:
📦 قطع الغيار (4)
├─ فرامل أمامية هيونداي
│   فرامل • منخفض • 12 قطعة
├─ فرامل خلفية تويوتا
│   فرامل • متوفر • 20 قطعة
└─ ...
```

### مثال 4: البحث برقم فاتورة
```
الإدخال: "INV-1001"

النتائج:
📄 الفواتير (1)
└─ INV-1001
    أحمد محمد • 1,500 ريال
```

---

## الأداء والتحسين

### ⚡ Live Search
```typescript
useEffect(() => {
  // البحث يتم فوراً عند تغيير النص
  if (!searchQuery.trim()) {
    setResults([]);
    return;
  }
  // ... البحث
}, [searchQuery, parts]);
```

### 🎯 البحث غير حساس لحالة الأحرف
```typescript
const query = searchQuery.toLowerCase();
part.name.toLowerCase().includes(query);
```

### 🔄 إعادة تعيين البحث عند الإغلاق
```typescript
const handleSelectResult = (result) => {
  setOpen(false);
  setSearchQuery(""); // ← تنظيف
  navigate(result.path);
};
```

---

## الحالات الخاصة

### 🔍 لا توجد نتائج
```
عرض رسالة:
"لا توجد نتائج
جرب كلمات بحث مختلفة"
```

### 📭 البحث فارغ
```
عرض رسالة ترحيب:
"ابحث في جميع البيانات
اكتب للبحث في قطع الغيار، العملاء، والفواتير"
```

### ❌ بيانات معطوبة في localStorage
```typescript
try {
  const data = JSON.parse(localStorage.getItem("customers"));
  return data || [];
} catch {
  return []; // إرجاع مصفوفة فارغة
}
```

---

## اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|----------|----------|
| `Ctrl+K` / `Cmd+K` | فتح البحث |
| `ESC` | إغلاق البحث |
| `↑` | التنقل للأعلى (قريباً) |
| `↓` | التنقل للأسفل (قريباً) |
| `Enter` | اختيار النتيجة (قريباً) |

---

## التطوير المستقبلي

يمكن إضافة:
- ✨ التنقل بالأسهم (↑↓) بين النتائج
- ✨ البحث في تفاصيل إضافية (الوصف، الملاحظات)
- ✨ ترتيب النتائج حسب الأهمية (Relevance)
- ✨ تاريخ البحث الأخير (Recent Searches)
- ✨ البحث المتقدم (Filters)
- ✨ عرض المزيد من النتائج (Load More)
- ✨ البحث الصوتي (Voice Search)

---

## أفضل الممارسات

### ✅ للمستخدمين

1. **استخدم Ctrl+K** للوصول السريع
2. **اكتب جزء من الاسم** فقط (لا حاجة للنص الكامل)
3. **ابحث برقم الهاتف** للعثور على العملاء بسرعة
4. **ابحث بالفئة** للعثور على جميع القطع المشابهة

### ✅ للمطورين

1. **تحديث sessionStorage** عند تنفيذ highlight
   ```typescript
   sessionStorage.setItem("highlightPart", id);
   ```

2. **قراءة sessionStorage** في الصفحات المستهدفة
   ```typescript
   const highlightId = sessionStorage.getItem("highlightPart");
   if (highlightId) {
     // تطبيق highlight
     sessionStorage.removeItem("highlightPart");
   }
   ```

3. **إضافة بيانات جديدة للبحث**
   - أضف نوع جديد في `SearchResult["type"]`
   - أضف منطق بحث في `useEffect`
   - أضف قسم جديد في عرض النتائج

---

## الأسئلة الشائعة

**س: هل البحث يعمل بدون إنترنت؟**
ج: نعم، البحث محلي بالكامل (في المتصفح).

**س: كم عدد النتائج القصوى؟**
ج: 15 نتيجة (يمكن زيادتها في الكود).

**س: هل يمكن البحث بالإنجليزية؟**
ج: نعم، إذا كانت البيانات بالإنجليزية.

**س: ماذا يحدث إذا ضغطت Ctrl+K مرتين؟**
ج: لا شيء، النافذة مفتوحة بالفعل.

**س: هل يحفظ تاريخ البحث؟**
ج: لا، حالياً لا يحفظ (يمكن إضافته مستقبلاً).

---

## الملخص

✅ **تم التنفيذ:**
- بحث شامل في قطع الغيار، العملاء، والفواتير
- اختصار Ctrl+K للوصول السريع
- نتائج مجمعة ومنظمة
- انتقال تلقائي للصفحة المناسبة
- واجهة سهلة وسريعة

✅ **النتيجة:**
بحث سريع واحترافي يوفر الوقت ويحسن تجربة المستخدم! 🚀
