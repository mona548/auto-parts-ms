# دليل نظام Local Storage

## نظرة عامة
تم إضافة نظام حفظ تلقائي كامل باستخدام Local Storage للمشروع. جميع البيانات تُحفظ تلقائياً في المتصفح ولا تُفقد عند تحديث الصفحة.

---

## الملفات المضافة

### 1. `/src/app/hooks/useLocalStorage.ts`
Custom hook لإدارة الحالة مع Local Storage.

**الوظائف الرئيسية:**
- `useLocalStorage<T>(key, initialValue)` - Hook رئيسي للحفظ التلقائي
- `clearAllAppData()` - مسح جميع البيانات المحفوظة
- `exportAppData()` - تصدير البيانات كملف JSON
- `importAppData(data)` - استيراد البيانات من ملف JSON

**مثال الاستخدام:**
```typescript
const [data, setData] = useLocalStorage('myKey', initialValue);
```

### 2. `/src/app/components/Settings.tsx`
صفحة الإعدادات الكاملة مع أدوات إدارة البيانات.

**الميزات:**
- ✅ تصدير البيانات (Export) - حفظ نسخة احتياطية JSON
- ✅ استيراد البيانات (Import) - استعادة نسخة احتياطية
- ✅ مسح البيانات (Clear) - حذف كل شيء والعودة للبيانات الافتراضية
- ✅ معلومات التخزين - حالة الحفظ التلقائي
- ✅ معلومات النظام - رقم الإصدار

---

## التعديلات على الملفات الموجودة

### 1. `/src/app/context/PartsContext.tsx`

**التغييرات:**
- ✅ استبدال `useState` بـ `useLocalStorage`
- ✅ نقل البيانات الافتراضية إلى متغيرات منفصلة (`initialParts`, `initialCategories`)
- ✅ تحسين `addPart` لإنشاء IDs فريدة بناءً على أعلى ID موجود

**قبل:**
```typescript
const [parts, setParts] = useState<Part[]>([...]);
```

**بعد:**
```typescript
const [parts, setParts] = useLocalStorage<Part[]>("parts", initialParts);
```

### 2. `/src/app/routes.tsx`

**التغييرات:**
- ✅ إضافة مسار `/settings` للإعدادات
- ✅ استيراد مكون `Settings`

### 3. `/src/app/components/Layout.tsx`

**التغييرات:**
- ✅ تحويل زر "الإعدادات" إلى `Link` يعمل
- ✅ يوجه المستخدم إلى `/settings`

---

## كيف يعمل النظام

### 1. الحفظ التلقائي
كل تغيير في البيانات يُحفظ **فوراً** في Local Storage:

```typescript
// عند إضافة قطعة جديدة
addPart(newPart);  // ← يُحفظ تلقائياً في localStorage

// عند تحديث قطعة
updatePart(id, updates);  // ← يُحفظ تلقائياً

// عند حذف قطعة
deletePart(id);  // ← يُحفظ تلقائياً
```

### 2. التحميل عند بدء التشغيل
عند فتح التطبيق:
1. يفحص `useLocalStorage` وجود بيانات محفوظة
2. إذا وُجدت → يحملها
3. إذا لم تُوجد → يستخدم البيانات الافتراضية

### 3. مفاتيح التخزين
البيانات تُحفظ تحت المفاتيح التالية:

| المفتاح | البيانات المحفوظة |
|---------|-------------------|
| `parts` | جميع قطع الغيار |
| `categories` | الفئات |
| `invoices` | الفواتير (مستقبلياً) |
| `customers` | العملاء (مستقبلياً) |

---

## استخدام صفحة الإعدادات

### 1. تصدير البيانات
```
الإعدادات → إدارة البيانات → تصدير
```
- يحفظ ملف JSON باسم: `parts-system-backup-2026-04-11.json`
- يحتوي على جميع البيانات (parts, categories, invoices, customers)
- مفيد للنسخ الاحتياطي أو النقل بين أجهزة

### 2. استيراد البيانات
```
الإعدادات → إدارة البيانات → استيراد → اختر ملف JSON
```
- يستعيد البيانات من ملف سابق
- يستبدل البيانات الحالية بالكامل
- يُعيد تحميل الصفحة تلقائياً

### 3. مسح البيانات
```
الإعدادات → إدارة البيانات → مسح البيانات → تأكيد
```
- يحذف **جميع** البيانات من Local Storage
- يعيد البيانات الافتراضية (8 قطع)
- يُعيد تحميل الصفحة تلقائياً

---

## معالجة الأخطاء

النظام يتعامل مع الحالات التالية:

### 1. Local Storage ممتلئ
```typescript
try {
  localStorage.setItem(key, value);
} catch (error) {
  console.error('Error saving data:', error);
  // يعرض رسالة للمستخدم
}
```

### 2. بيانات تالفة في Local Storage
```typescript
try {
  const data = JSON.parse(localStorage.getItem(key));
} catch (error) {
  console.warn('Corrupted data, using defaults');
  return initialValue;
}
```

### 3. المتصفح لا يدعم Local Storage
```typescript
if (typeof window === "undefined") {
  return initialValue;
}
```

---

## نصائح للاستخدام

### ✅ الحفاظ على البيانات
- لا تمسح بيانات المتصفح إذا كنت تريد الاحتفاظ بالمعلومات
- اعمل نسخة احتياطية (Export) بانتظام

### ✅ النقل بين الأجهزة
1. في الجهاز الأول: الإعدادات → تصدير
2. انقل ملف JSON
3. في الجهاز الثاني: الإعدادات → استيراد

### ✅ إعادة البدء من الصفر
الإعدادات → مسح البيانات → تأكيد

### ⚠️ محدودية المساحة
- Local Storage محدود بـ 5-10 MB في معظم المتصفحات
- كافي لآلاف القطع والفواتير
- إذا امتلأت المساحة، ستظهر رسالة خطأ

---

## التطوير المستقبلي

يمكن إضافة:
- ✨ مزامنة السحابة (Cloud Sync)
- ✨ تصدير PDF/Excel
- ✨ نسخ احتياطي تلقائي
- ✨ نسخ احتياطي متعدد
- ✨ ضغط البيانات لتوفير المساحة

---

## أسئلة شائعة

**س: هل البيانات آمنة؟**
ج: نعم، محفوظة محلياً في جهازك فقط. لا تُرسل لأي خادم.

**س: ماذا يحدث عند مسح بيانات المتصفح؟**
ج: تُحذف جميع البيانات المحفوظة. تأكد من عمل نسخة احتياطية قبل ذلك.

**س: هل يمكن استخدام النظام على أجهزة متعددة؟**
ج: نعم، باستخدام ميزة التصدير/الاستيراد لنقل البيانات.

**س: ماذا يحدث إذا أغلقت المتصفح؟**
ج: لا شيء! البيانات محفوظة وستكون موجودة عند فتح المتصفح مرة أخرى.

---

## الملخص

✅ **تم التنفيذ:**
- نظام حفظ تلقائي كامل
- صفحة إعدادات احترافية
- تصدير/استيراد البيانات
- معالجة أخطاء شاملة

✅ **لا توجد تغييرات في:**
- الواجهة الحالية
- TypeScript interfaces
- وظائف الصفحات الأخرى

✅ **النتيجة:**
البيانات الآن محفوظة تلقائياً ولن تُفقد عند تحديث الصفحة! 🎉
