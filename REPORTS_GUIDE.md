# دليل صفحة التقارير والإحصائيات

## نظرة عامة
صفحة تقارير شاملة توفر تحليلات مفصلة لأداء النظام عبر 4 تقارير رئيسية.

---

## الملفات المضافة

### 1. `/src/app/components/Reports.tsx`
صفحة التقارير الكاملة مع جميع الرسوم البيانية والجداول.

### 2. تحديث `/src/app/routes.tsx`
- إضافة المسار `/reports`

### 3. تحديث `/src/app/components/Layout.tsx`
- إضافة "التقارير" في Sidebar مع أيقونة BarChart2

---

## التقارير الأربعة الرئيسية

### 📈 **التقرير الأول: تقرير المبيعات**

**الوصف:** رسم بياني خطي يعرض إجمالي المبيعات خلال فترة زمنية محددة.

**المكونات:**
1. **الرسم البياني (LineChart)**
   - المحور X: التاريخ (يوم/شهر)
   - المحور Y: المبلغ بالعملة المحددة
   - خط أزرق بسمك 3px
   - نقاط على كل يوم

2. **فلتر الفترة الزمنية:**
   - آخر 7 أيام
   - آخر 30 يوم (افتراضي)
   - آخر 3 أشهر

3. **بطاقات ملخص:**
   - **إجمالي المبيعات** (أزرق)
   - **متوسط المبيعات اليومي** (أخضر)
   - **أعلى يوم مبيعات** (بنفسجي)

**البيانات:**
```typescript
// يستخرج من localStorage
const invoices = JSON.parse(localStorage.getItem("invoices") || "[]");

// يحسب المبيعات لكل يوم
salesData = [
  { date: "1/4", sales: 1500 },
  { date: "2/4", sales: 2300 },
  ...
]
```

**مثال الاستخدام:**
```
1. افتح صفحة التقارير → /reports
2. اختر "تقرير المبيعات"
3. اختر الفترة: "آخر 30 يوم"
4. شاهد الرسم البياني
5. اضغط "طباعة التقرير"
```

---

### 📦 **التقرير الثاني: تقرير المخزون**

**الوصف:** رسم بياني عمودي يعرض الكميات حسب الفئة والحالة + جدول القطع المنخفضة.

**المكونات:**
1. **الرسم البياني (BarChart)**
   - المحور X: الفئة (فلاتر، فرامل، كهرباء...)
   - المحور Y: الكمية
   - 3 أعمدة لكل فئة:
     - 🟢 **متوفر** (أخضر #22C55E)
     - 🟠 **منخفض** (برتقالي #F59E0B)
     - 🔴 **ينفذ** (أحمر #EF4444)

2. **جدول القطع المنخفضة:**
   - مرتب من الأقل كمية للأكثر
   - الأعمدة:
     - الكود
     - الاسم
     - الفئة
     - الكمية
     - الحالة (Badge ملون)

**البيانات:**
```typescript
// من PartsContext
const { parts } = useParts();

// يجمع حسب الفئة والحالة
inventoryData = [
  { category: "فلاتر", متوفر: 45, منخفض: 12, ينفذ: 3 },
  { category: "فرامل", متوفر: 20, منخفض: 8, ينفذ: 5 },
  ...
]

// القطع المنخفضة
lowStockParts = parts
  .filter(p => p.status === "ينفذ" || p.status === "منخفض")
  .sort((a, b) => a.quantity - b.quantity);
```

**حالات الاستخدام:**
- 📊 مراقبة المخزون لكل فئة
- ⚠️ تحديد القطع التي تحتاج إعادة طلب
- 📋 طباعة قائمة الطلبات

---

### 👥 **التقرير الثالث: تقرير العملاء**

**الوصف:** أفضل 10 عملاء مع رسم دائري لتوزيع المبيعات.

**المكونات:**
1. **جدول أفضل 10 عملاء:**
   - مرتب حسب إجمالي المشتريات (من الأعلى للأقل)
   - الأعمدة:
     - الترتيب (1-10)
     - اسم العميل
     - عدد الفواتير
     - إجمالي المشتريات (أزرق)
     - المدفوع (أخضر)
     - المتبقي (أحمر)

2. **الرسم الدائري (PieChart):**
   - يعرض أفضل 5 عملاء فقط
   - كل قسم بلون مختلف
   - يعرض النسبة المئوية
   - Legend لأسماء العملاء

**البيانات:**
```typescript
// من localStorage
const customers = JSON.parse(localStorage.getItem("customers") || "[]");
const invoices = JSON.parse(localStorage.getItem("invoices") || "[]");

// يحسب إحصائيات كل عميل
topCustomers = [
  {
    name: "أحمد محمد",
    invoiceCount: 15,
    total: 45000,
    paid: 40000,
    remaining: 5000
  },
  ...
].sort((a, b) => b.total - a.total).slice(0, 10);
```

**الألوان المستخدمة:**
```typescript
const COLORS = [
  "#3B82F6", // أزرق
  "#22C55E", // أخضر
  "#F59E0B", // برتقالي
  "#EF4444", // أحمر
  "#A855F7"  // بنفسجي
];
```

---

### 💰 **التقرير الرابع: ملخص مالي**

**الوصف:** بطاقات ملخص + جدول الفواتير غير المسددة.

**المكونات:**
1. **4 بطاقات إحصائية:**

   **📊 إجمالي المبيعات** (أزرق)
   ```
   المبلغ الكلي لجميع الفواتير
   أيقونة: DollarSign
   ```

   **✅ إجمالي المحصّل** (أخضر)
   ```
   المبلغ المدفوع من جميع الفواتير
   أيقونة: DollarSign
   ```

   **❌ إجمالي المتبقي** (أحمر)
   ```
   المبلغ غير المحصّل
   أيقونة: DollarSign
   ```

   **📄 عدد الفواتير** (بنفسجي)
   ```
   العدد الكلي للفواتير
   أيقونة: FileText
   ```

2. **جدول الفواتير غير المسددة:**
   - مرتب من الأقدم للأحدث (حسب التاريخ)
   - الأعمدة:
     - رقم الفاتورة
     - اسم العميل
     - التاريخ
     - الإجمالي
     - المدفوع (أخضر)
     - المتبقي (أحمر)
     - الحالة (Badge: جزئية/غير مدفوعة)

**البيانات:**
```typescript
// الملخص المالي
financialSummary = {
  totalSales: invoices.reduce((sum, inv) => sum + inv.total, 0),
  totalPaid: invoices.reduce((sum, inv) => sum + inv.paid, 0),
  totalRemaining: invoices.reduce((sum, inv) => sum + inv.remaining, 0),
  invoiceCount: invoices.length
};

// الفواتير غير المسددة
unpaidInvoices = invoices
  .filter(inv => inv.remaining > 0)
  .sort((a, b) => new Date(a.date) - new Date(b.date));
```

**الحالات في الجدول:**
- 🟠 **جزئية**: `remaining > 0 && paid > 0`
- 🔴 **غير مدفوعة**: `remaining > 0 && paid === 0`

---

## التنقل بين التقارير (Tabs)

### واجهة التبويبات

```
┌──────────────────────────────────────────────────────┐
│  📈 تقرير المبيعات  │  📦 تقرير المخزون  │ ...     │
└──────────────────────────────────────────────────────┘
```

**التبويبات الأربعة:**
1. 📈 **تقرير المبيعات** - TrendingUp icon
2. 📦 **تقرير المخزون** - Package icon
3. 👥 **تقرير العملاء** - Users icon
4. 💰 **ملخص مالي** - DollarSign icon

**السلوك:**
- التبويب النشط: لون أزرق + خط سفلي
- التبويبات الأخرى: رمادي + hover effect
- transition سلس عند التبديل

---

## ميزة الطباعة

### زر "طباعة التقرير"

**الموقع:** أعلى يمين كل تقرير

**السلوك:**
```typescript
const handlePrint = () => {
  window.print();
};
```

**CSS للطباعة:**
```css
@media print {
  /* إخفاء الأزرار */
  .print\:hidden { display: none; }
  
  /* ضبط المسافات */
  .print\:p-4 { padding: 1rem; }
}
```

**ما يُطبع:**
- ✅ عنوان التقرير
- ✅ الرسوم البيانية
- ✅ الجداول
- ✅ البطاقات الإحصائية
- ❌ الأزرار والتبويبات
- ❌ Sidebar والـ Header

**أفضل الممارسات:**
1. اختر التقرير المطلوب
2. اختر الفلتر (إن وُجد)
3. اضغط "طباعة التقرير"
4. في نافذة الطباعة:
   - اختر الطابعة أو "حفظ PDF"
   - اختر الاتجاه: عمودي/أفقي
   - اضبط الهوامش
5. اطبع أو احفظ

---

## الرسوم البيانية (Recharts)

### مكتبة Recharts

**المكونات المستخدمة:**
```typescript
import {
  LineChart,    // تقرير المبيعات
  BarChart,     // تقرير المخزون
  PieChart,     // تقرير العملاء
  XAxis,        // المحور الأفقي
  YAxis,        // المحور الرأسي
  CartesianGrid,// الشبكة
  Tooltip,      // التلميحات
  Legend,       // وصف الألوان
} from "recharts";
```

### إعدادات مشتركة

**Tooltip (التلميحات):**
```typescript
<Tooltip
  contentStyle={{
    backgroundColor: "white",
    border: "1px solid #e5e7eb",
    borderRadius: "0.5rem",
    boxShadow: "var(--shadow-md)"
  }}
  formatter={(value) => `${value} ${settings.currency}`}
/>
```

**CartesianGrid (الشبكة):**
```typescript
<CartesianGrid strokeDasharray="3 3" stroke="#e5e7eb" />
```

**Axes (المحاور):**
```typescript
<XAxis stroke="#6b7280" style={{ fontSize: "0.875rem" }} />
<YAxis stroke="#6b7280" style={{ fontSize: "0.875rem" }} />
```

---

## العملة الديناميكية

جميع المبالغ تستخدم العملة من الإعدادات:

```typescript
import { useSettings } from "../context/SettingsContext";

const { settings } = useSettings();

// في العرض
{financialSummary.totalSales.toFixed(2)} {settings.currency}
// النتيجة: "45,000.00 ريال" أو "جنيه" أو "دينار"
```

---

## الأداء والتحسين

### useMemo للحسابات الثقيلة

```typescript
// يُحسب مرة واحدة ويُحفظ في الذاكرة
const salesData = useMemo(() => {
  // حسابات معقدة...
  return data;
}, [invoices, salesPeriod]); // يُعاد الحساب فقط عند تغيير هذه القيم
```

**البيانات المُحسّنة:**
- `salesData` - بيانات الرسم البياني للمبيعات
- `inventoryData` - بيانات المخزون
- `lowStockParts` - القطع المنخفضة
- `topCustomers` - أفضل العملاء
- `customerPieData` - بيانات الرسم الدائري
- `financialSummary` - الملخص المالي
- `unpaidInvoices` - الفواتير غير المسددة

---

## أمثلة الاستخدام

### مثال 1: مراجعة المبيعات الشهرية
```
1. افتح /reports
2. التبويب النشط: "تقرير المبيعات" (افتراضي)
3. اختر الفلتر: "آخر 30 يوم"
4. شاهد الرسم البياني
5. راجع البطاقات الإحصائية
6. اضغط "طباعة" لحفظ PDF
```

### مثال 2: التحقق من المخزون المنخفض
```
1. افتح /reports
2. اختر التبويب: "تقرير المخزون"
3. راجع الرسم البياني العمودي
4. تصفح جدول "القطع التي وصلت للحد الأدنى"
5. أطلب القطع من المورد
6. اطبع القائمة
```

### مثال 3: تحديد أفضل العملاء
```
1. افتح /reports
2. اختر التبويب: "تقرير العملاء"
3. راجع الجدول (أفضل 10)
4. شاهد الرسم الدائري (أفضل 5)
5. حدد العملاء للعروض الخاصة
```

### مثال 4: متابعة الفواتير غير المسددة
```
1. افتح /reports
2. اختر التبويب: "ملخص مالي"
3. راجع البطاقات الأربعة
4. تصفح جدول "الفواتير غير المسددة"
5. اتصل بالعملاء لتحصيل المتبقي
```

---

## الحالات الخاصة

### 📭 لا توجد بيانات
```typescript
if (invoices.length === 0) {
  // الرسم البياني يعرض خط فارغ
  // الجداول فارغة
  // البطاقات تعرض 0
}
```

### 📊 بيانات قليلة
```typescript
// أقل من 10 عملاء
topCustomers.slice(0, 10); // يعرض ما هو موجود فقط

// أقل من 5 عملاء للرسم الدائري
customerPieData.slice(0, 5); // نفس الشيء
```

### 🔢 أرقام كبيرة
```typescript
// التنسيق بفاصلتين عشريتين
{value.toFixed(2)} {settings.currency}
// النتيجة: "123,456.78 ريال"
```

---

## الألوان المستخدمة

| العنصر | اللون | Hex |
|--------|-------|-----|
| المبيعات (خط) | أزرق | #3B82F6 |
| متوفر (عمود) | أخضر | #22C55E |
| منخفض (عمود) | برتقالي | #F59E0B |
| ينفذ (عمود) | أحمر | #EF4444 |
| بنفسجي (بطاقة) | بنفسجي | #A855F7 |

---

## التطوير المستقبلي

يمكن إضافة:
- ✨ تصدير التقارير كـ PDF/Excel
- ✨ تقرير الأرباح (المبيعات - التكلفة)
- ✨ تقرير الموردين
- ✨ مقارنة الفترات (هذا الشهر vs الشهر الماضي)
- ✨ تقارير مخصصة (Custom Reports)
- ✨ جدولة التقارير (Email تلقائي)
- ✨ رسم بياني للتوقعات (Predictions)

---

## الأسئلة الشائعة

**س: كيف أطبع تقرير معين؟**
ج: اختر التبويب المطلوب → اضغط "طباعة التقرير" → اختر "حفظ PDF" أو الطباعة.

**س: هل يمكن عرض فترات مخصصة؟**
ج: حالياً فقط 7/30/90 يوم. يمكن إضافة Date Range Picker مستقبلاً.

**س: من أين تأتي البيانات؟**
ج: من PartsContext (قطع الغيار) و localStorage (فواتير وعملاء).

**س: هل التقارير تُحدّث تلقائياً؟**
ج: نعم، تستخدم useMemo وتُحدّث عند تغيير البيانات.

**س: كيف أغير العملة المعروضة؟**
ج: من الإعدادات → إعدادات الفواتير → العملة.

---

## الملخص

✅ **تم التنفيذ:**
- 4 تقارير شاملة ومفصلة
- رسوم بيانية احترافية (Line, Bar, Pie)
- جداول تفاعلية مع بيانات كاملة
- فلترة زمنية للمبيعات
- طباعة سهلة لكل تقرير
- استخدام العملة من الإعدادات
- تصميم responsive وسهل الاستخدام

✅ **النتيجة:**
نظام تقارير متكامل يوفر رؤية شاملة لأداء النظام! 📊✨
