# دليل صفحة الإعدادات

## نظرة عامة
صفحة إعدادات شاملة لإدارة جميع جوانب نظام إدارة قطع الغيار.

---

## الملفات الجديدة المضافة

### 1. `/src/app/context/SettingsContext.tsx`
Context لإدارة إعدادات النظام مع Local Storage.

**الواجهة الرئيسية (ShopSettings):**
```typescript
interface ShopSettings {
  // معلومات المتجر
  shopName: string;
  phoneNumber: string;
  address: string;

  // إعدادات المخزون
  defaultMinQuantity: number;
  enableLowStockAlerts: boolean;

  // إعدادات الفواتير
  currency: "ريال" | "جنيه" | "دينار" | "درهم";
  defaultInvoiceNote: string;
  invoiceStartNumber: number;
}
```

**الوظائف المتاحة:**
- `settings` - الإعدادات الحالية
- `updateSettings(newSettings)` - تحديث الإعدادات
- `resetSettings()` - إعادة تعيين للإعدادات الافتراضية

---

## الأقسام الأربعة في صفحة الإعدادات

### 📍 القسم الأول: معلومات المتجر

**الحقول:**
1. **اسم المتجر** (shopName)
   - القيمة الافتراضية: "ورشة الأمانة لقطع الغيار"
   - يظهر في رأس الفواتير المطبوعة

2. **رقم الهاتف** (phoneNumber)
   - القيمة الافتراضية: "0500000000"
   - يظهر في الفواتير المطبوعة

3. **العنوان** (address)
   - القيمة الافتراضية: "الرياض، المملكة العربية السعودية"
   - يظهر في الفواتير المطبوعة

**الغرض:**
هذه المعلومات تُستخدم في طباعة الفواتير لتعريف المتجر للعملاء.

---

### 📦 القسم الثاني: إعدادات المخزون

**الحقول:**
1. **الحد الأدنى الافتراضي للتنبيه** (defaultMinQuantity)
   - القيمة الافتراضية: 5
   - النطاق: 1 وما فوق
   - **كيف يعمل:**
     - إذا كانت الكمية ≤ الحد الأدنى → حالة "ينفذ" 🔴
     - إذا كانت الكمية ≤ (الحد الأدنى × 3) → حالة "منخفض" 🟠
     - إذا كانت الكمية > (الحد الأدنى × 3) → حالة "متوفر" 🟢

2. **تفعيل تنبيهات المخزون المنخفض** (enableLowStockAlerts)
   - القيمة الافتراضية: مفعّل (true)
   - Toggle switch
   - عند التفعيل: يظهر تنبيهات للقطع منخفضة المخزون

**مثال:**
```
الحد الأدنى = 5

الكمية: 3  → حالة: "ينفذ" (أحمر)
الكمية: 10 → حالة: "منخفض" (برتقالي)
الكمية: 20 → حالة: "متوفر" (أخضر)
```

---

### 💰 القسم الثالث: إعدادات الفواتير

**الحقول:**
1. **العملة** (currency)
   - القيمة الافتراضية: "ريال"
   - الخيارات المتاحة:
     - ريال (السعودية)
     - جنيه (مصر)
     - دينار (الأردن، الكويت، البحرين)
     - درهم (الإمارات)
   - تظهر في جميع الفواتير المطبوعة

2. **رقم بداية الفواتير** (invoiceStartNumber)
   - القيمة الافتراضية: 1000
   - النطاق: 1 وما فوق
   - **كيف يعمل:**
     - الفاتورة الأولى: INV-1000
     - الفاتورة الثانية: INV-1001
     - وهكذا...

3. **ملاحظة افتراضية في الفواتير** (defaultInvoiceNote)
   - القيمة الافتراضية: "شكراً لتعاملكم معنا"
   - نص حر (textarea)
   - تظهر تلقائياً في نهاية كل فاتورة مطبوعة
   - يمكن تعديلها لكل فاتورة على حدة

**استخدام العملة:**
```typescript
// في الفواتير
const total = 1500;
const currency = settings.currency; // "ريال"
const display = `${total} ${currency}`; // "1500 ريال"
```

---

### 🗂️ القسم الرابع: إدارة البيانات

**الوظائف الثلاث:**

#### 1. تصدير البيانات (Export)
```
الإعدادات → إدارة البيانات → تصدير
```

**ما يحدث:**
- يحفظ ملف JSON باسم: `parts-system-backup-2026-04-12.json`
- يحتوي على:
  - جميع القطع (parts)
  - الفئات (categories)
  - الفواتير (invoices)
  - العملاء (customers)
  - **إعدادات المتجر (shopSettings)** ← جديد!

**محتوى الملف:**
```json
{
  "parts": [...],
  "categories": [...],
  "invoices": [...],
  "customers": [...],
  "shopSettings": {
    "shopName": "ورشة الأمانة لقطع الغيار",
    "phoneNumber": "0500000000",
    "currency": "ريال",
    ...
  },
  "exportedAt": "2026-04-12T10:30:00.000Z"
}
```

#### 2. استيراد البيانات (Import)
```
الإعدادات → إدارة البيانات → استيراد → اختر ملف JSON
```

**ما يحدث:**
- يقرأ ملف JSON
- يستبدل **جميع** البيانات الحالية
- يشمل استعادة إعدادات المتجر
- يُعيد تحميل الصفحة تلقائياً

⚠️ **تحذير:** الاستيراد يستبدل جميع البيانات الحالية!

#### 3. مسح جميع البيانات (Clear)
```
الإعدادات → إدارة البيانات → مسح البيانات → تأكيد
```

**ما يحدث:**
- يحذف من Local Storage:
  - parts
  - categories
  - invoices
  - customers
  - shopSettings
- يعيد البيانات الافتراضية
- يُعيد تحميل الصفحة

---

## التكامل مع بقية النظام

### استخدام الإعدادات في مكونات أخرى

**مثال 1: استخدام اسم المتجر في الفواتير**
```typescript
import { useSettings } from "../context/SettingsContext";

function InvoicePrint() {
  const { settings } = useSettings();
  
  return (
    <div>
      <h1>{settings.shopName}</h1>
      <p>{settings.phoneNumber}</p>
      <p>{settings.address}</p>
    </div>
  );
}
```

**مثال 2: استخدام العملة**
```typescript
function InvoiceTotal({ total }: { total: number }) {
  const { settings } = useSettings();
  
  return (
    <p>{total} {settings.currency}</p>
  );
}
```

**مثال 3: استخدام الحد الأدنى للمخزون**
```typescript
function AddPart() {
  const { settings } = useSettings();
  
  const defaultMinQty = settings.defaultMinQuantity; // 5
  
  // استخدمه كقيمة افتراضية في النموذج
}
```

---

## Local Storage

### المفاتيح المحفوظة
| المفتاح | البيانات |
|---------|----------|
| `shopSettings` | إعدادات المتجر الكاملة |
| `parts` | قطع الغيار |
| `categories` | الفئات |
| `invoices` | الفواتير |
| `customers` | العملاء |

### التحديث التلقائي
كل تغيير في الإعدادات يُحفظ فوراً في Local Storage:
```typescript
// عند الضغط على "حفظ الإعدادات"
updateSettings(formData); // ← يُحفظ تلقائياً في localStorage
```

---

## واجهة المستخدم

### الأيقونات المستخدمة
- 🏪 **Store** - معلومات المتجر (أزرق)
- 📦 **Package** - إعدادات المخزون (بنفسجي)
- 📄 **FileText** - إعدادات الفواتير (أخضر)
- 💾 **Download** - تصدير البيانات
- 📤 **Upload** - استيراد البيانات
- 🗑️ **Trash2** - مسح البيانات
- ✅ **CheckCircle** - نجاح العملية
- ⚠️ **AlertCircle** - تحذير أو خطأ

### الألوان
- **أزرق (#3B82F6)** - الأزرار الرئيسية، معلومات المتجر
- **بنفسجي (#A855F7)** - إعدادات المخزون
- **أخضر (#22C55E)** - إعدادات الفواتير، نجاح
- **أحمر (#EF4444)** - مسح البيانات، أخطاء
- **رمادي (#6B7280)** - معلومات ثانوية

---

## الإشعارات (Notifications)

### أنواع الإشعارات

**1. نجاح (Success)** 🟢
```
✅ تم حفظ الإعدادات بنجاح!
✅ تم تصدير البيانات بنجاح!
✅ تم استيراد البيانات بنجاح!
✅ تم إعادة تعيين الإعدادات!
```

**2. خطأ (Error)** 🔴
```
❌ فشل تصدير البيانات!
❌ فشل استيراد البيانات!
❌ ملف غير صالح!
```

**مدة العرض:** 3 ثوانٍ ثم تختفي تلقائياً

---

## الحماية والتحقق

### التحقق من المدخلات
```typescript
// الحد الأدنى للمخزون
min="1"  // لا يقبل أصفار أو أرقام سالبة

// رقم بداية الفواتير
min="1"  // يجب أن يكون موجباً

// رقم الهاتف
type="tel"  // يعرض لوحة مفاتيح الأرقام على الموبايل
```

### تأكيد العمليات الخطرة
- **مسح البيانات** → يعرض Dialog تأكيد
- **استيراد البيانات** → يستبدل البيانات فوراً (بدون تأكيد)

---

## أفضل الممارسات

### ✅ للمستخدمين

1. **احفظ نسخة احتياطية بانتظام**
   ```
   الإعدادات → تصدير → مرة كل أسبوع
   ```

2. **لا تمسح البيانات إلا إذا كنت متأكداً**
   - اعمل نسخة احتياطية أولاً
   - تأكد من عدم وجود فواتير مهمة

3. **اضبط الحد الأدنى للمخزون حسب احتياجك**
   - قطع سريعة الحركة → حد أدنى أعلى (10)
   - قطع بطيئة الحركة → حد أدنى أقل (3)

### ✅ للمطورين

1. **استخدم `useSettings()` في أي مكون**
   ```typescript
   const { settings, updateSettings } = useSettings();
   ```

2. **لا تعدل `settings` مباشرة**
   ```typescript
   // ❌ خطأ
   settings.shopName = "اسم جديد";
   
   // ✅ صحيح
   updateSettings({ shopName: "اسم جديد" });
   ```

3. **استخدم القيم الافتراضية من الإعدادات**
   ```typescript
   const minQty = settings.defaultMinQuantity; // بدلاً من 5 ثابتة
   ```

---

## التطوير المستقبلي

يمكن إضافة:
- ✨ إعدادات الطباعة (حجم الخط، الهوامش)
- ✨ إعدادات الألوان (ثيم فاتح/داكن)
- ✨ إعدادات التنبيهات (بريد إلكتروني، SMS)
- ✨ إعدادات الصلاحيات (للمستخدمين المتعددين)
- ✨ إعدادات الضرائب (VAT/ضريبة القيمة المضافة)

---

## أسئلة شائعة

**س: هل تؤثر الإعدادات على البيانات الموجودة؟**
ج: لا، الإعدادات تؤثر فقط على العمليات الجديدة (فواتير جديدة، قطع جديدة).

**س: ماذا يحدث إذا غيرت العملة؟**
ج: الفواتير القديمة تبقى بالعملة القديمة، الفواتير الجديدة تستخدم العملة الجديدة.

**س: كيف أنقل الإعدادات لجهاز آخر؟**
ج: استخدم التصدير/الاستيراد - الإعدادات مضمنة في ملف النسخة الاحتياطية.

**س: هل يمكن إعادة تعيين إعداد واحد فقط؟**
ج: حالياً لا، زر "إعادة تعيين" يعيد جميع الإعدادات للقيم الافتراضية.

---

## الملخص

✅ **تم التنفيذ:**
- 4 أقسام كاملة للإعدادات
- حفظ تلقائي في Local Storage
- تصدير/استيراد يشمل الإعدادات
- واجهة سهلة ومنظمة
- إشعارات واضحة للعمليات

✅ **النتيجة:**
نظام إعدادات شامل قابل للتخصيص بالكامل! 🎉
